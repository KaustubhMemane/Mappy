<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="actionBarStyle">@style/ThemeActionBar</item>
        <item name="android:windowFullscreen">false</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="actionOverflowButtonStyle">@style/MyActionBarButtonOverflow</item>

    </style>

    
    <style name="ThemeActionBar"
        parent="Widget.AppCompat.Light.ActionButton.Overflow">
        <item name="background">@null</item>
        <item name="android:actionOverflowMenuStyle">@drawable/ic_search_holo_light</item>
     </style>

    <style name="MyActionBarButtonOverflow" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:tint">@color/colorBlack</item>
    </style>
</resources>
